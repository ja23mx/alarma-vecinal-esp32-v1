# Ruta al header con la versión del firmware (fuente única de verdad)
HEADER_PATH = "../include/CnfTarjeta.h"
# Carpeta destino para el archivo OTA (último build, sobreescribe)
OTA_DIR = "../esp32-bin-files/ota"
# Carpeta de versiones liberadas (historial por versión)
RELEASES_DIR = "../esp32-bin-files/releases"
